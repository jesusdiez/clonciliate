(ns clonciliate.core
  (:gen-class))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))

(defn parseOperationalLine
  "Generates a map from the operationalLine"
  [line]
  (str line))
